import styleSlider from '../scss/slider.scss';

function Slider() {
  return (
    <div className={styleSlider.wrapper}>
      <div className={styleSlider.slider}>

      </div>
    </div>
        );
        }

        export default Slider;